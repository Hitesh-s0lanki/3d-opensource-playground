"""Request, response and file-format models.

`scene.py` is the odd one out: it is not an HTTP model at all but the on-disk
scene.json contract shared with Blender, which is a schema in the same sense.
"""
